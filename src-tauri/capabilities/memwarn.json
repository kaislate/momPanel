{
  "$schema": "../gen/schemas/desktop-schema.json",
  "identifier": "memwarn",
  "description": "Capability for the escalation-modal window shown when high-memory alerts are ignored.",
  "windows": ["memwarn"],
  "permissions": [
    "core:default",
    "core:event:default",
    "core:window:allow-hide",
    "core:window:allow-close"
  ]
}
