{
  "$schema": "../gen/schemas/desktop-schema.json",
  "identifier": "memwarn",
  "description": "Capability for the always-on-top high-memory warning banner window.",
  "windows": ["memwarn"],
  "permissions": [
    "core:default",
    "core:event:default",
    "core:window:allow-hide",
    "core:window:allow-close"
  ]
}
